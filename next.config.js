/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      unoptimized: false,
      domains: [
        'cdn.darkmaterial.space'
      ]
    }
  };
  
  module.exports = nextConfig;